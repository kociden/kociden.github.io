const fs=require('fs'),path=require('path');const root=__dirname;let errors=[],count=0;for(let n=1;n<=10;n++)for(const l of ['ja','en','zh']){let b=path.join(root,'concepts',String(n).padStart(2,'0'),'site',l);let files=['index.html','about/index.html','news/index.html','announcements/index.html','partners/index.html','contact/index.html','security-policy/index.html','news/care-information-platform-support-2026/index.html','news/operations-started-2026/index.html','news/company-founded-2026/index.html'];for(let f of files){let p=path.join(b,f);if(!fs.existsSync(p))errors.push('missing '+p);else{count++;let h=fs.readFileSync(p,'utf8');for(let m of h.matchAll(/(?:href|src)="([^"#]+)"/g)){let u=m[1];if(/^(https?:|mailto:)/.test(u))continue;let q=path.resolve(path.dirname(p),u);if(!fs.existsSync(q))errors.push(`${p}: broken ${u}`)}}}}console.log(`${count} HTML files checked; ${errors.length} errors`);if(errors.length){console.error(errors.slice(0,30).join('\n'));process.exit(1)}
